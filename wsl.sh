# wslview: is the equivalent of xdg-open for wsl distributions
wslview file.txt
wslview directory

# we can also open the windows terminal and open the files from there by typing the path of the file. To exit the cmd just type "exit"
cmd.exe

# or open the windows file manager
explorer.exe
explorer.exe directory

# we can install wsl-open as an alternative
npm install -g wsl-open
